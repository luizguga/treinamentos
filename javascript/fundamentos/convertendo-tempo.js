var dias = 10
var horas = dias * 24
var minutos = horas * 60
var segundos = minutos * 60

console.log(`${dias} dias tem ${horas} horas, que em minutos são ${minutos}, que em segundos são ${segundos}`)